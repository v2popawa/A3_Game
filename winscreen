function drawWin() {
  background(0);

  fill(0, 255, 0);
  textAlign(CENTER);
  textSize(40);
  text("You Solved All Cases!", width / 2, height / 2);

  textSize(20);
  text("Click to Restart", width / 2, height / 2 + 50);
}

function winMousePressed() {
  currentScreen = "start";
}
